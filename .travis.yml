language: python
python:
  - "3.5"
# command to install dependencies
install: 
  - "pip install -r requirements.txt"
  - mkdir /var/log/hb
  - chown $USER /var/log/hb

# command to run tests
script: 
  - python manage.py test

